version: '3.8'
services:
  app:
    build: .
    ports:
      - "4000:4000"
    environment:
      - REDIS_URL=redis://redis:6379
      - SESSION_SECRET=supersecret
      - NODE_ENV=development
    depends_on:
      - redis
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
